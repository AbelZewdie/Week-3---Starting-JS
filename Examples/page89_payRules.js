//Exercise

//Given
let regularHour = 40;
let overTime = regularHour * 1.5;

// Inputs
let payRate = 12.50;
let hoursWorked = 20;

//Expressions
const isOver = hoursWorked > regularHour;
const isUnder = hoursWorked < regularHour;

//Outputs
let grossPay, reason;

grossPay = payRate * hoursWorked;
bonusRate = ;

if(hoursWorked > 40){
  reason = "IS OVER LIMIT.";
 grossPay = payRate * overTime;

}else if